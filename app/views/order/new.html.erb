<h1>Shopping Cart</h1>

<table>
  <tr>
    <td></td>
    <td>Product Name</td>
    <td>Price</td>
  </tr>
  <tr>

    <%session["cart"].each_with_index do |product, index| %>
    <td>
      <!-- <%=link_to "remove", update_cart_path(index), method: "post" %> <br /> -->
      <button class="remove" id='<%=index%>' > Remove</button>
    </td>
    <td><%=product.name%></td>
    <td><%=product.price%></td>
    <td></td>
    <%end%>
  </tr>

</table>

</br>

<a href="/checkout"><button>Make Purchase</button></a>

